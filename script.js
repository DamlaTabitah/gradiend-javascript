const obj = ([1, 2, 3, 4, 5]);
let sum = 0;
obj.forEach(num => {
    sum += num;
});
console.log(sum);


const obj = {a:1, b:2, C:3, e:4, f:5};
let sum = 0;
obj.forEach(num => {
    sum += num;
});
console.log(sum);
forEach();

const obj = {
    a: 1, 
    b: 2,
    C: 3,
    e: 4, 
    f: 5
};

let sum = 0;
for ( let key in Object => {
    return sum += obj[key];
});
const obj = {
    a: 1, 
    b: 2,
    C: 3,
    e: 4, 
    f: 5
};

let sum = 0;
for ( let key in Object => {
    return sum += obj[key];
});

